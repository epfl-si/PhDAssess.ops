# To push this playbook to OpenShift, say
#
#   ansible-playbook playbook.yml -i inventory/dev.yml

- name: Phd Assess on OpenShift
  hosts: all
  gather_facts: no   # Not yet
  roles:
    - role: roles/phd-assess
