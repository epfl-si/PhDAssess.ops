- name: Zeebe metrics reader
  kubernetes.core.k8s:
    definition:
      apiVersion: monitoring.coreos.com/v1
      kind: PodMonitor
      metadata:
        name: zeebe-monitoring
        namespace: '{{ openshift_namespace }}'
      spec:
        selector:
          matchLabels:
            app.kubernetes.io/name: 'zeebe'
        podMetricsEndpoints:
          - port: "metrics"
            path: '/actuator/prometheus'
            interval: '15s'
          - port: "metrics"
            path: '/metrics'
            interval: '30s'

- name: Grafana base folder
  ansible.builtin.include_tasks:
    file: grafana.yml

- name: Zeebe data dashboard definition
  kubernetes.core.k8s:
    definition:
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: 'zeebe-grafana-dashboard'
        namespace: '{{ openshift_namespace }}'
      data:
        json: >
          {{ lookup('file', 'templates/grafana-dashboards/zeebe.json') | string }}

- name: Zeebe data dashboard in Grafana
  kubernetes.core.k8s:
    definition:
      apiVersion: grafana.integreatly.org/v1beta1
      kind: GrafanaDashboard
      metadata:
        name: 'zeebe'
        namespace: '{{ openshift_namespace }}'
      spec:
        resyncPeriod: 30s
        allowCrossNamespaceImport: true
        instanceSelector:
          matchLabels:
            dashboards: '{{ grafana_dashboard_instance }}'
        folder: "{{ grafana_folder_name }}"
        configMapRef:
          key: json
          name: 'zeebe-grafana-dashboard'
